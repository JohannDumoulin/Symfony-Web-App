{% extends 'base.html.twig' %}

{% block title %}Hello UserAccountController!{% endblock %}

{% block content %}

    <div class="prose">
        <h1>{{ 'user.my_account'|trans }}</h1>

        <p class="font-semibold text-lg">{{ 'hello'|trans }} {{ app.user.email }} !</p>
    </div>

    <a href="{{ path('app_logout') }}" class="btn mt-8">{{ 'logout'|trans }}</a>

{% endblock %}
